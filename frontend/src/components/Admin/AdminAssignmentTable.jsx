import { Link } from 'react-router-dom'
import AdminIcon from './AdminIcons.jsx'

export default function AdminAssignmentTable({ assignments, deletingId, onDelete }) {
  return <div className="admin-assignment-table-wrap"><table className="admin-assignment-table"><thead><tr><th>Assignment</th><th>Course</th><th>Module / Lesson</th><th>Due Date</th><th>Max Marks</th><th>Submissions</th><th>Graded</th><th><span className="sr-only">Actions</span></th></tr></thead><tbody>{assignments.map((assignment) => <tr key={assignment._id}><td><Link className="admin-assignment-table__title" to={`/admin/assignments/${assignment._id}`}>{assignment.title}</Link><span className={`admin-status-pill ${assignment.isPublished ? 'is-published' : 'is-draft'}`}>{assignment.isPublished ? 'Published' : 'Draft'}</span></td><td>{assignment.courseTitle}</td><td><strong>{assignment.moduleTitle}</strong><span>{assignment.lessonTitle}</span></td><td><time dateTime={assignment.dueDate}>{new Date(assignment.dueDate).toLocaleDateString()}</time></td><td>{assignment.maxMarks}</td><td>{assignment.submissionCount}</td><td>{assignment.gradedCount}</td><td><div className="admin-table-actions"><Link to={`/admin/assignments/${assignment._id}`} aria-label={`View ${assignment.title}`}><AdminIcon name="eye" size={17} /></Link><Link to={`/admin/assignments/${assignment._id}/submissions`} aria-label={`View submissions for ${assignment.title}`}><AdminIcon name="users" size={17} /></Link><Link to={`/admin/assignments/${assignment._id}/edit`} aria-label={`Edit ${assignment.title}`}><AdminIcon name="edit" size={17} /></Link><button type="button" disabled={deletingId === assignment._id} onClick={() => onDelete(assignment)} aria-label={`Delete ${assignment.title}`}><AdminIcon name="trash" size={17} /></button></div></td></tr>)}</tbody></table></div>
}
