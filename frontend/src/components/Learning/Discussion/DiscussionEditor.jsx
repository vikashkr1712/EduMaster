import { useState } from 'react'
export default function DiscussionEditor({ onPost, busy }) { const [question, setQuestion] = useState(''); const submit = async () => { if (!question.trim()) return; await onPost(question.trim()); setQuestion('') }; return <div className="lms-discussion-editor"><label htmlFor="discussion-question">Ask a Question</label><textarea id="discussion-question" value={question} onChange={(event) => setQuestion(event.target.value)} placeholder="What would you like help understanding?" maxLength={3000} /><button type="button" className="lms-primary" onClick={submit} disabled={busy || question.trim().length < 3}>{busy ? 'Posting…' : 'Post Question'}</button></div> }
