export default function NotesList({ notes, onEdit, onDelete }) { return <div className="lms-notes-list">{notes.length ? notes.map((note) => <article key={note._id}><header><h4>{note.title}</h4><div><button type="button" onClick={() => onEdit(note)}>Edit</button><button type="button" onClick={() => window.confirm('Delete this note?') && onDelete(note._id)}>Delete</button></div></header><p>{note.content}</p><small>Updated {new Intl.DateTimeFormat('en-IN', { day: '2-digit', month: 'short', hour: '2-digit', minute: '2-digit' }).format(new Date(note.updatedAt))}</small></article>) : <div className="lms-empty"><span>✎</span><h3>No notes found</h3><p>Create a note or adjust your search.</p></div>}</div> }
